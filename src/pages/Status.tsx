import { Link, useNavigate } from 'react-router-dom'

export function NotFound() { return <Status title="Page not found" message="The page you requested does not exist." action="Go home" to="/" /> }
export function Unauthorized() { return <Status title="Access denied" message="You do not have permission to view this workspace area." action="Back to dashboard" to="/dashboard" /> }
export function ForgotPassword() { const navigate = useNavigate(); return <main className="grid min-h-screen place-items-center bg-canvas p-6"><section className="max-w-md rounded-panel border border-line bg-surface p-8 text-center"><h1 className="text-2xl font-bold text-ink">Password assistance</h1><p className="mt-3 text-sm leading-6 text-secondary">Contact your workspace administrator to reset your password.</p><button onClick={() => navigate('/login')} className="mt-6 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-white">Return to sign in</button></section></main> }
function Status({ title, message, action, to }: { title: string; message: string; action: string; to: string }) { return <main className="grid min-h-screen place-items-center bg-canvas p-6"><section className="max-w-md rounded-panel border border-line bg-surface p-8 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-indigo-50 text-lg font-bold text-primary">O</div><h1 className="mt-5 text-2xl font-bold text-ink">{title}</h1><p className="mt-3 text-sm leading-6 text-secondary">{message}</p><Link to={to} className="mt-6 inline-block rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-white">{action}</Link></section></main> }
